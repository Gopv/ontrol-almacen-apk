name: myapp
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Dependencias del sistema de almacén
  firebase_core: ^2.32.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.0
  mobile_scanner: ^3.5.5
  qr_flutter: ^4.1.0
  csv: ^6.0.0
  share_plus: ^7.2.2
  path_provider: ^2.1.1
  image_picker: ^1.2.2
  firebase_storage: ^11.7.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true